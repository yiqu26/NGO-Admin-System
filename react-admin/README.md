# React 管理後台（員工系統）

> NGO 案件管理系統 - 員工、督導、管理員使用的管理後台

---

## 📋 系統簡介

這是一個基於 **React 18 + Vite** 開發的現代化管理後台系統，供 NGO 組織的員工、督導和管理員使用。

### 🎯 適用對象

- **管理員**：完整系統權限
- **督導**：管理和審核權限
- **員工**：基本操作權限

---

## 🛠 技術棧

- **框架**：React 18
- **建置工具**：Vite
- **UI 框架**：Material-UI (MUI) / Ant Design
- **狀態管理**：React Context API
- **HTTP 客戶端**：Axios
- **圖表**：Chart.js / Recharts
- **路由**：React Router v6

---

## ✨ 核心功能

### 📊 Dashboard 儀表板
- 即時統計數據（個案、志工、活動）
- 性別分佈圖表
- 城市分佈地圖
- 困難類型分析
- 近期活動概覽

### 📝 案件管理
- 新增/編輯/刪除個案
- 進階搜尋與篩選
- 個案狀態追蹤
- 個案歷史記錄
- 語音輸入功能（Azure Speech）

### 🎯 活動管理
- 活動建立與編輯
- 參與人數管理
- 活動行程安排
- 活動成效追蹤
- AI 活動優化建議

### 📦 物資管理
- 物資庫存管理
- 物資分類系統
- 物資價值統計
- 低庫存警示
- 物資發放記錄

### 👥 帳號管理
- 員工帳號管理
- 角色權限設定
- 帳號啟用/停用
- 密碼重置

### 📅 行程管理
- 個人行程管理
- 行程提醒通知
- 團隊行程檢視

---

## 🚀 快速啟動

### 環境需求

- Node.js 18+
- npm 或 yarn

### 安裝步驟

```bash
# 安裝依賴
npm install

# 開發模式啟動
npm run dev

# 建置生產版本
npm run build

# 預覽生產版本
npm run preview
```

### 環境變數設定

複製 `.env.example` 並重命名為 `.env.development`：

```bash
cp .env.example .env.development
```

編輯 `.env.development`：

```env
# API 位址
VITE_API_BASE_URL=http://localhost:5264/api

# Azure Speech Service（語音輸入功能）
VITE_AZURE_SPEECH_KEY=your-key-here
VITE_AZURE_SPEECH_REGION=eastasia

# Google Maps（地圖選擇器）
VITE_GOOGLE_MAPS_API_KEY=your-key-here
```

---

## 🔐 測試帳號

請參考：[docs/03-測試帳號.md](../docs/03-測試帳號.md)

| 角色 | Email | 密碼 |
|------|-------|------|
| 管理員 | admin@ngo.org | Admin123! |
| 督導 | supervisor@ngo.org | Super123! |
| 員工 | staff@ngo.org | Staff123! |

---

## 📁 專案結構

```
frontend/
├── src/
│   ├── components/          # React 元件
│   │   ├── ActivityManagementPage/
│   │   ├── CaseManagementPage/
│   │   ├── SuppliesManagementPage/
│   │   ├── layout/          # 佈局元件
│   │   └── shared/          # 共用元件
│   ├── pages/               # 頁面元件
│   │   ├── Dashboard.tsx
│   │   ├── CaseManagement.tsx
│   │   ├── ActivityManagement.tsx
│   │   └── ...
│   ├── services/            # API 服務
│   │   ├── caseManagement/
│   │   ├── activityManagement/
│   │   └── supplyManagement/
│   ├── contexts/            # 狀態管理
│   │   ├── AuthProvider.tsx
│   │   └── NotificationContext.tsx
│   ├── utils/               # 工具函數
│   ├── styles/              # 樣式文件
│   └── main.tsx             # 應用程式入口
├── public/                  # 靜態資源
├── .env.development         # 開發環境變數
├── .env.production          # 生產環境變數
├── vite.config.ts           # Vite 配置
├── package.json
└── README.md                # 本文件
```

---

## 🌐 運行位址

- **開發模式**：http://localhost:5173
- **需要後端 API**：http://localhost:5264

---

## 🔗 相關連結

- [主要 README](../README.md) - 專案總覽
- [快速啟動指南](../QUICK-START.md) - 完整啟動步驟
- [測試帳號](../docs/03-測試帳號.md) - 測試帳號與權限
- [後端 API](../backend/README.md) - WebAPI 說明

---

## 📝 開發指南

### 新增頁面

1. 在 `src/pages/` 創建新的頁面元件
2. 在 `src/routes/index.tsx` 註冊路由
3. 在 Sidebar 新增導航連結

### 新增 API 服務

1. 在 `src/services/` 對應模組新增服務函數
2. 使用 `api.ts` 中的 axios 實例
3. 統一錯誤處理

### 權限控制

使用 `useUserRole()` hook 檢查用戶權限：

```tsx
import { useUserRole } from '@/hooks/useUserRole';

const { isAdmin, isSupervisor, isStaff } = useUserRole();

{isAdmin && <AdminOnlyButton />}
```

---

## 🎨 UI 設計規範

- **主色調**：藍色系（#1976d2）
- **響應式設計**：支援桌面與平板
- **無障礙**：遵循 WCAG 2.1 AA 標準

---

## 📦 主要依賴

```json
{
  "react": "^18.x",
  "react-router-dom": "^6.x",
  "@mui/material": "^5.x",
  "antd": "^5.x",
  "chart.js": "^4.x",
  "axios": "^1.x"
}
```

---

## 🚨 常見問題

### 問題 1: 無法連接 API

**解決**：確認後端 API 已啟動（http://localhost:5264）

### 問題 2: 登入失敗

**解決**：檢查測試帳號密碼是否正確（區分大小寫）

### 問題 3: 環境變數未生效

**解決**：重新啟動開發伺服器（npm run dev）

---

**最後更新**：2025-12-29
